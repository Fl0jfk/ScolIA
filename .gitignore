node_modules

# clerk configuration (can include secrets)
/.clerk/

# environnement local (ne jamais pousser de secrets)
.env
.env.*
!.env.example
!.env.local.example
.env.local
.cursor/mcp.local.env
.cursor/tools/.last-screenshot.png
.cursor/tools/.browser-runner.cjs
.next/

# caches / bruit local
.DS_Store
tsconfig.tsbuildinfo

# copies locales du registry tenant (secrets → S3 scolia-registry uniquement)
/index.json
/la-providence-nicolas-barre.json
tenants/secrets/*.json
*-secrets.json

