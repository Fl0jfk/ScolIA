import FamilleNotesClient from "@/app/components/famille/FamilleNotesClient";

export const metadata = {
  title: "Notes — Espace famille",
};

export default function FamilleNotesPage() {
  return <FamilleNotesClient />;
}
