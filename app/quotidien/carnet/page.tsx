import FamilleCarnetClient from "@/app/components/famille/FamilleCarnetClient";

export const metadata = {
  title: "Carnet — Portail quotidien",
};

export default function Page() {
  return <FamilleCarnetClient />;
}
