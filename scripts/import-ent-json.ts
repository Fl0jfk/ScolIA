/**
 * @deprecated Le miroir tenant_document a été retiré.
 * Utiliser : npm run ent:migrate-relational -- --tenant=<slug>
 */
console.error(
  "ent:import-json est obsolète. Utilisez npm run ent:migrate-relational -- --tenant=<slug>",
);
process.exit(1);
