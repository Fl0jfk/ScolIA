/**
 * @deprecated ent_entity a été droppé.
 * Utiliser : npm run ent:migrate-relational -- --tenant=<slug>
 */
console.error(
  "ent:normalize est obsolète. Utilisez npm run ent:migrate-relational -- --tenant=<slug>",
);
process.exit(1);
